// routes/articles.js
const express = require('express');
const router = express.Router();

// Tambahkan rute terkait artikel di sini nanti
// Contoh:
// router.get('/', (req, res) => {
//   res.send('Articles route works!');
// });

module.exports = router;
